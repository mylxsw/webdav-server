prefix: "/works"
scope: "/Users/mylxsw/Workstation"
noSniff: false
# 目录访问权限： 空-没有权限，R-只读，RW-读写
access: "RW"
userAccess: "R"

rules:
  - path: /library/
    # 特定目录读写权限，默认无权限
    access: "RW"

userRules:
  - path: "/data"
    access: "RW"
    users:
      - admin
      - editor
